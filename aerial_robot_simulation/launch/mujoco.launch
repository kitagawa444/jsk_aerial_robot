<?xml version="1.0" encoding="utf-8"?>
<launch>
  <arg name="robot_ns" default="" />
  <arg name="headless" default="false" />
  <arg name="mujoco_model" default="" />
  <arg name="launch_backend" default="true" />
  <arg name="launch_controller" default="true" />

  <!-- mujoco ros control -->
  <include file="$(find mujoco_ros_control)/launch/mujoco.launch" if="$(arg launch_backend)" >
    <arg name="robot_ns" value="$(arg robot_ns)" />
    <arg name="headless" value="$(arg headless)"/>
    <arg name="mujoco_model" value="$(arg mujoco_model)"/>
  </include>

  <!-- basic parameter -->
  <!-- Load this before the shared multi-robot backend starts.  In that setup this
       launch file is first included with both launch flags disabled only to
       prepare each robot namespace. -->
  <rosparam file="$(find aerial_robot_simulation)/config/Mujoco.yaml" command="load" ns="$(arg robot_ns)" />

  <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false"
        output="screen" ns="$(arg robot_ns)" args="--timeout 120 attitude_controller" if="$(arg launch_controller)" />

</launch>
